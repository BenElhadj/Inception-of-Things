Vagrant.configure("2") do |config|
  config.vm.box = "ubuntu/focal64"

  config.vm.define "bhamdiS" do |server|
    server.vm.hostname = "bhamdiS"
    server.vm.network "private_network", ip: "192.168.56.110"

    server.vm.provider "virtualbox" do |vb|
      vb.memory = "2048"
      vb.cpus = 1
      vb.name = "bhamdiS"
    end

    server.vm.synced_folder "~/p1", "/vagrant_data"
  end

  config.vm.define "bhamdiSW" do |worker|
    worker.vm.hostname = "bhamdiSW"
    worker.vm.network "private_network", ip: "192.168.56.111"

    worker.vm.provider "virtualbox" do |vb|
      vb.memory = "2048"
      vb.cpus = 1
      vb.name = "bhamdiSW"
    end

    worker.vm.synced_folder "~/p1", "/vagrant_data"
  end

  config.vm.provision "shell", inline: <<-SHELL
    sudo apt-get update
    sudo apt-get upgrade -y
  SHELL
end
