echo 'plugins=(git
            zsh-autosuggestions
            )' >> ~/.zshrc
******************************
echo 'export VBOX_USER_HOME="/mnt/nfs/homes/bhamdi/sgoinfre/Inception-of-Things/.configVB"' >> ~/.zshrc
echo 'export VAGRANT_HOME="/mnt/nfs/homes/bhamdi/sgoinfre/Inception-of-Things/.vagrant.d"' >> ~/.zshrc
echo 'export VBOX_MSI_INSTALL_PATH="/mnt/nfs/homes/bhamdi/sgoinfre/Inception-of-Things/VirtualBoxVMs"' >> ~/.zshrc
echo 'export VBOX_MSI_INSTALL_PATH="/mnt/nfs/homes/bhamdi/sgoinfre/Inception-of-Things/VirtualBoxVMs"' >> ~/.bashrc

source ~/.zshrc
assurer vous d'avoir ton VirtualBox ouvert
########################################

vagrant up / vagrant reload 
vagrant ssh 

scp -r ../../vagrant/bonus ../../vagrant/p* . && cd p1 && vagrant up
vagrant reload 

vagrant ssh bhamdiS
vagrant ssh bhamdiSW

sudo chmod 644 /etc/rancher/k3s/k3s.yaml

vagrant@bhamdiS:~$ kubectl get nodes -o wide

vagrant halt -f && vagrant destroy -f && rm -rf .configVB .vagrant .vagrant.d VirtualBoxVMs ~/VirtualBox\ VMs


pour appliquer des modification sur la machine deja creer
vagrant halt
vagrant up
vagrant provision


vagrant global-status 
rm -rf ~/.vagrant.d


cp -rf .bash_logout .bash_logoutOld && cp -rf .bashrc .bashrcOld && cp -rf .cache .cacheOld/ && cp -rf .profile .profileOld


en cas de probleme dans la machine vagrant@Inseption-Of-Things pour eviter de tout reinstaller

vagrant halt -f && vagrant destroy -f && rm -rf .configVB .vagrant .vagrant.d VirtualBoxVMs ~/VirtualBox\ VMs
cd && rm -rf p* bonus/ VirtualBox\ VMs/ .config/ .vagrant.d/
cp -rf .bash_logoutOld .bash_logout && cp -rf .bashrcOld .bashrc && cp -rf .cacheOld/ .cache && cp -rf .profileOld .profile
scp -r ../../vagrant/bonus ../../vagrant/p* . && cd p1 && vagrant up



pour tester le p1

cd p1
vagrant up

vagrant ssh bhamdiS
dans un autre terminale et au memme dossier
vagrant ssh bhamdiSW

k get nodes -o wide

k describe node bhamdiS
k describe node bhamdiSW

ifconfig eth1

vagrant halt -f && vagrant destroy -f && rm -rf .configVB .vagrant .vagrant.d VirtualBoxVMs ~/VirtualBox\ VMs ./scripts  



pour tester le p2